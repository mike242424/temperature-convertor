import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  root: { flex: 1, justifyContent: 'center', padding: 50 },
  container: {
    height: 450,
    alignItems: 'center',
    justifyContent: 'space-evenly',
  },
  backgroundImage: {
    height: '100%',
  },
});
