import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  button: {
    backgroundColor: 'black',
    padding: 15,
    borderRadius: 30,
  },
  buttonText: {
    color: 'white',
    fontWeight: 'bold',
  },
});
